services:
  yt-dlp-webui:
    image: ghcr.io/marcopeocchi/yt-dlp-web-ui:latest
    container_name: yt-dlp-webui
    ports:
      - "3033:3033"
    volumes:
      - "/docker/ytdlpwebui:/downloads" # Local directory to store downloaded files
      - "/docker/ytdlpwebui:/config" # Local directory for configuration (optional)
    command:
      - --qs=2 # Limiting to 2 concurrent downloads
      - --port=3033
      - --host=0.0.0.0
