services:
  plex:
    image: lscr.io/linuxserver/plex:latest
    container_name: plex
    network_mode: host
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Pacific/Auckland
      - VERSION=docker
    volumes:
      - /docker/plex/library:/config
      - /docker/deluge/downloads:/deluge
    restart: unless-stopped
